import React from 'react'

// keys are 'urls', values are 'base64' representations of the image
export const NETWORK_IMAGE_CACHE = {}

export const navigationRef = React.createRef()
export const isReadyRef = React.createRef()